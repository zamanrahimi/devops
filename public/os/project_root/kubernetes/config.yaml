apiVersion: v1
kind: ConfigMap
metadata:
  name: php-app-config
data:
  IMAGE_TAG: "v2.29"
  schema.sql: |
    CREATE TABLE empt (
      id INT,
      name TEXT
    );

    INSERT INTO empt VALUES (1, 'Ali');